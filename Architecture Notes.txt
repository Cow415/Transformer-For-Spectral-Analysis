Structure Feed
Baseline Correction
       ↓
Normalization
       ↓
1D Convolutional Autoencoder
       ↓
16–32 dimensional latent space
       ↓
Classifier or clustering model


Processing Pipeline
Raw Data
    ↓
Loader
    ↓
Preprocessing
    ↓
Dataset
    ↓
Model
    ↓
Training
    ↓
Evaluation
    ↓
Benchmark
    ↓
Inference


Common practice: 
==================
history = {
    "train": [],
    "val": []
}

best_val = np.inf
for epoch in range(epochs):

    train_loss = ...

    val_loss = ...

    log_metrics(...)

    scheduler.step(val_loss)

    if val_loss < best_val:

        best_val = val_loss

        save_checkpoint(...)

    if early_stop(val_loss):

        break